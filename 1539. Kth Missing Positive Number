class Solution:
    def findKthPositive(self, arr: List[int], k: int) -> int:
        a=[]
        for i in range(1,5000):
            a.append(i)
        for i in arr:
            a.remove(i)
        return a[k-1]
